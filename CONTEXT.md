# Glossary (คำศัพท์เฉพาะ)

- **Raw Source (แหล่งข้อมูลดิบ)**: เอกสารที่ไม่สามารถเปลี่ยนแปลงได้ (เช่น บทความ, เปเปอร์, บันทึก) ที่ผู้ใช้กำหนดมาให้ โดยจะต้องเก็บไว้ในโฟลเดอร์ `raw/` อย่างเคร่งครัด LLM จะอ่านจากไฟล์เหล่านี้แต่จะไม่แก้ไขมันอย่างเด็ดขาด
- **Wiki (วิกิ)**: ชุดไฟล์ markdown แบบมีโครงสร้างที่ถูกสร้างและดูแลรักษาโดย LLM ทั้งหมด ใช้ **Structured Directory Structure (โครงสร้างไดเรกทอรีแบบมีโครงสร้าง)** (เช่น `wiki/entities/`, `wiki/concepts/`, `wiki/sources/`) เพื่อรักษาความเป็นระเบียบเมื่อมีขนาดใหญ่ขึ้น ไฟล์ต่างๆ จะต้องถูกวางไว้ในโฟลเดอร์ย่อยตามประเภทของมันเสมอ และห้ามวางไว้ในไดเรกทอรีราก (root directory) อย่างเด็ดขาด
- **Schema (สคีมา/โครงสร้าง)**: กฎและธรรมเนียมปฏิบัติที่เป็นตัวชี้นำพฤติกรรมของ LLM ในการดูแลรักษา Wiki (ในโปรเจกต์นี้ ไฟล์ `CONTEXT.md` และพรอมต์ที่เกี่ยวข้องจะทำหน้าที่เป็น Schema)
- **Ingest (การนำเข้าข้อมูล)**: กระบวนการที่ผู้ใช้สั่งให้ LLM อ่าน Raw Source ใหม่จากโฟลเดอร์ `raw/` อภิปรายประเด็นสำคัญ และอัปเดต Wiki ตามโครงสร้างเพื่อสะท้อนถึงความรู้ใหม่ สถานะการ Ingest จะถูกติดตามผ่านการมีอยู่ของแหล่งข้อมูลนั้นในไฟล์ `index.md` และหน้า Wiki ที่เกี่ยวข้องเท่านั้น ไม่ใช่โดยการย้ายไฟล์ดิบ
- **Index (`index.md`) (ดัชนี)**: ไฟล์ markdown ข้อความธรรมดาแบบสแตติกที่ทำหน้าที่จัดทำแคตตาล็อกทุกอย่างใน Wiki แอดเจนต์ (Agent) จะอัปเดตไฟล์นี้ด้วยตนเองในทุกครั้งที่มีการ Ingest (แทนที่จะใช้ปลั๊กอินแบบไดนามิกเช่น Dataview) เพื่อให้ Agent สามารถอ่านได้อย่างรวดเร็วในฐานะแผนที่ของฐานความรู้
- **Wiki Lint (Health Check) (การตรวจสอบสุขภาพวิกิ)**: เวิร์กโฟลว์การบำรุงรักษาที่ Agent จะสแกนฐานความรู้ในขอบเขตที่จำกัดอย่างเคร่งครัด (อนุญาตเฉพาะไฟล์และโฟลเดอร์ต่อไปนี้: `wiki/` ยกเว้น `wiki/meta/`, ไฟล์ `index.md`, `hot.md`, และ `logs/log.md` ตัวปัจจุบันเท่านั้น ห้ามสแกนหาในระดับ Root แบบครอบจักรวาล) เพื่อระบุลิงก์ที่เสีย (Dead links), การกล่าวถึงที่ไม่ได้ลิงก์ (Unlinked mentions), หน้ากำพร้า (Orphan pages), และพร็อพเพอร์ตี้ที่หายไป (Missing properties) โดยไฟล์ `hot.md`, `index.md`, และ `logs/log.md` จะได้รับการยกเว้นจากการตรวจสอบ Frontmatter อย่างชัดเจน และ `logs/log.md` จะถูกตรวจสอบเฉพาะประเด็นลิงก์เสียเท่านั้น กระบวนการนี้จะสร้างไฟล์ชั่วคราว `lint-report.md` (ไว้ที่ `wiki/meta/`) เพื่อใช้สร้าง Orchestration Plan ในการแก้ไขปัญหาอย่างเป็นระบบ **Zero-Warning Policy**: เราใช้กฎไม่อนุโลมปัญหาใดๆ หากการตรวจสอบพบปัญหา Agent จะต้องเสนอให้รัน `wiki-lint-fix` ทันทีเพื่อเคลียร์ปัญหาให้เป็น 0 เสมอ และเมื่อแก้ปัญหาเสร็จสิ้น จะต้อง **ลบ** ทั้งไฟล์ Report และไฟล์ Orchestration Plan ทิ้งทันทีเพื่อรักษาความสะอาดของโฟลเดอร์
- **Log (`log.md`) (บันทึก)**: บันทึกตามลำดับเวลาของการดำเนินการทั้งหมด (เช่น Ingest, Query, Lint) ที่ Agent ทำ ใช้รูปแบบ Audit Trace (การติดตามการตรวจสอบ) ที่มีรายละเอียด (มักใช้ตารางเพื่อให้อ่านง่าย) เพื่อแสดงรายการว่าหน้า Wiki ใดบ้างที่ถูกสร้างหรือแก้ไขระหว่างการดำเนินการ
- **Log Rotation (การหมุนเวียนบันทึก)**: กระบวนการเก็บถาวร (Archive) บันทึกเก่าๆ เมื่อ `log.md` มีรายการเกิน 100 รายการ Agent จะตัดและวางบันทึกที่เก่าที่สุดลงในไฟล์เก็บถาวร (เช่น `logs/archive-YYYY-MM.md`) เพื่อให้บันทึกหลักอ่านง่ายและยังคงประวัติเอาไว้
- **Hot Cache (`hot.md`) (แคชร้อน)**: ไฟล์หน่วยความจำขนาดเล็ก (ประมาณ 500 คำ) ที่อยู่ในไดเรกทอรีราก ทำหน้าที่เป็นหน่วยความจำระยะสั้นของ Agent โดยสรุปบริบทล่าสุด, งานที่กำลังทำอยู่ (`Current Focus`), ไฟล์ที่เปิดอยู่ (`Active Files`), การตัดสินใจที่เปิดอยู่ (`Decisions in Flight`), และการเปลี่ยนแปลงล่าสุด Agent จะอ่านไฟล์นี้ก่อนเป็นอันดับแรกก่อนที่จะคิวรี Wiki ทั้งหมดเพื่อลดการใช้โทเค็นและดึงบริบทกลับมาได้อย่างรวดเร็ว **Lifecycle (วงจรชีวิต):** Agent จะอัปเดตไฟล์นี้เฉพาะเมื่อสิ้นสุดงานหลักหรือเฟสของเวิร์กโฟลว์ (ไม่ใช่หลังจากทุกพรอมต์) เพื่อลดความซ้ำซ้อนและประหยัดโทเค็น **Eviction (การนำออก):** รายการ 'Recent Changes' (การเปลี่ยนแปลงล่าสุด) ที่เก่าเกินกว่า 3-5 รายการล่าสุด จะถูกลบออกเพื่อให้ไฟล์สั้นกระชับ เนื่องจากการกระทำในอดีตถูกเก็บไว้ใน `log.md` แล้ว
- **Knowledge Synthesis (การสังเคราะห์ความรู้)**: แนวทางหลักในการจัดการเมื่อข้อมูลใหม่มีเนื้อหาสอดคล้องหรือทับซ้อนกับหน้า Wiki ที่มีอยู่แล้ว Agent จะต้องนำข้อมูลใหม่ไปผสม (Merge) เข้ากับโครงสร้างเนื้อหาเดิม (Synthesized Source of Truth) แทนที่จะเขียนต่อท้ายแบบบันทึก (Append-only log) และหากพบข้อมูลที่ขัดแย้งกัน ให้ใช้หลักการ **Preserve & Compare** (เก็บทั้งสองมุมมอง) แทนการเขียนทับ (Overwrite) เพื่อสะท้อนความหลากหลายของความรู้
- **Agent (แอดเจนต์/ตัวแทน)**: ผู้ช่วย LLM เชิงสนทนาที่ทำหน้าที่เป็น "โปรแกรมเมอร์" หรือ "ผู้ดูแล" ของ Wiki โดยทำงานร่วมกับ IDE ของผู้ใช้ (Obsidian)
- **Subagent (แอดเจนต์ย่อย)**: อินสแตนซ์ของ LLM ที่มีความเชี่ยวชาญเฉพาะด้าน ซึ่งถูกเรียกใช้โดย Agent หลักเพื่อทำงานเฉพาะเจาะจง (เช่น `verifier` (ผู้ตรวจสอบ) สำหรับ peer review, หรือการประมวลผลไฟล์คู่ขนาน) Subagent จะทำงานอยู่เบื้องหลังและรายงานกลับไปยัง Agent หลักเมื่อทำเสร็จ
- **Page Template (เทมเพลตหน้า)**: หน้า Wiki ในระบบจะถูกแบ่งออกเป็นหลายประเภท (เช่น `concept`, `entity`, `source`) ซึ่งแต่ละประเภทจะมีเทมเพลตเฉพาะของตัวเองเก็บไว้ในโฟลเดอร์ `_templates/` (เช่น `_templates/concept.md`) Agent จะต้องอ้างอิงและคัดลอก YAML frontmatter และหัวข้อมาตรฐานจากเทมเพลตที่ตรงกับ `type` ของหน้านั้นๆ เสมอเมื่อสร้างหรืออัปเดตหน้า โครงสร้างของหน้า (หัวข้อระดับ `#` และ `##`) จะต้องตรงกับที่กำหนดไว้ในเทมเพลตเท่านั้น ห้ามคิดค้นหัวข้อหลักขึ้นมาเองเด็ดขาด แต่สามารถสร้างหัวข้อย่อยระดับ `###` ลงไปได้ตามความเหมาะสมของเนื้อหา หากส่วนใดไม่มีข้อมูล (เช่น ยังไม่มีลิงก์ที่เกี่ยวข้อง) ให้ Agent ลบหัวข้อนั้นทิ้งไปเลย ไม่ต้องใส่หัวข้อเปล่าๆ ทิ้งไว้ และไม่ต้องเขียนว่า `- None` อีกต่อไป
- **Skill (ทักษะ)**: ความสามารถอัตโนมัติเฉพาะทางหรือทริกเกอร์ที่เป็นจุดเริ่มต้น (เช่น `defuddle`, `wiki-ingest`) ที่ Agent สามารถเรียกใช้เพื่อทำงานเฉพาะเจาะจง ทักษะสามารถถูกใช้แบบอิสระหรือถูกเรียกจากภายใน Workflow
- **Workflow (เวิร์กโฟลว์/ขั้นตอนการทำงาน)**: กระบวนการจัดการ (Orchestration) แบบหลายขั้นตอน (เช่น "Ingest Workflow" ที่กำหนดไว้ใน `.agents/workflows/ingestion.md`) ที่กำหนดเฟสที่มีโครงสร้าง, ต้องการ Checkpoints (จุดตรวจสอบ) ที่ชัดเจนจากผู้ใช้, และสามารถมอบหมายงานให้กับ Subagents แบบขนาน เวิร์กโฟลว์ทำหน้าที่เป็นแหล่งข้อมูลอ้างอิงหลัก (single source of truth) สำหรับกระบวนการ และมักใช้หลายทักษะ (Skills) ในการทำแต่ละขั้นตอนให้สำเร็จ
- **Orchestration Plan (แผนการจัดการ)**: ไฟล์ markdown (เก็บไว้ในโฟลเดอร์ `plans/`) ที่ใช้เพื่อแยกย่อยและติดตามการดำเนินการของงานที่ซับซ้อน (เช่น Ingestion) ออกเป็นเฟสที่แยกจากกันและติดตามได้ แต่ละเฟสจะต้องผ่านการตรวจสอบ (Verification / Peer Review) และรับจุดตรวจสอบ (Checkpoint) อย่างชัดเจนจากผู้ใช้ก่อนที่จะดำเนินการต่อ
  - **Granular Task Breakdown (การแยกย่อยงานอย่างละเอียด)**: แผนจะต้องแยกย่อยงานสำหรับแต่ละเฟสออกเป็นงานย่อยที่มีรายละเอียดเล็กที่สุดเท่าที่จะเป็นไปได้ เพื่อให้สามารถแจกจ่ายและมอบหมายงานให้กับ Subagent ได้
  - **Single-File Scope Constraint (ข้อจำกัดขอบเขตไฟล์เดียว)**: งานหนึ่งๆ จะต้องไม่ครอบคลุมหลายไฟล์พร้อมกัน งานแต่ละชิ้นจะต้องถูกกำหนดขอบเขตให้กำหนดเป้าหมายไปที่ไฟล์เฉพาะหนึ่งไฟล์เท่านั้นอย่างชัดเจน
  - **Progress Tracking (การติดตามความคืบหน้า)**: Main Agent (Orchestrator) จะต้องใช้เครื่องมือแก้ไขไฟล์ (เช่น `replace_file_content`) เพื่ออัปเดตไฟล์ Orchestration Plan โดยติ๊กเครื่องหมาย (`- [x]`) หน้างานย่อย (Granular Task) หรือ Checkpoint ทันทีที่ปฏิบัติงานในส่วนนั้นเสร็จสมบูรณ์ หากมีการใช้ Subagent ให้ Subagent รายงานผลกลับมายัง Main Agent และ Main Agent จะเป็นผู้เดียวที่มีสิทธิ์ในการแก้ไขไฟล์ Plan เพื่อป้องกันปัญหาการเซฟทับกัน (Concurrency conflicts) ห้ามละทิ้งสถานะ `- [ ]` ไว้หลังจากทำงานเสร็จแล้วเด็ดขาด
  - **Failure Handling (การจัดการข้อผิดพลาด)**: หากงานย่อยล้มเหลว ให้ Main Agent ทำเครื่องหมายแสดงความล้มเหลว (เช่น เปลี่ยนเป็น `- [!]` หรือพิมพ์สาเหตุต่อท้าย) ใน Orchestration Plan และข้ามไปทำงานย่อยอื่นในเฟสนั้นต่อไปจนจบ จากนั้นจึงสรุปข้อผิดพลาดทั้งหมดให้ผู้ใช้ทราบเมื่อถึง Checkpoint (ห้ามหยุดการทำงานทั้งเฟสเพียงเพราะงานย่อยเดียวล้มเหลว)
  - **Staging Phase (เฟสเตรียมการ)**: เฟสตามแนวคิดใน Orchestration Plan ที่ Agent ได้ร่างการเปลี่ยนแปลงหรือหน้าใหม่ไว้แล้ว โดยจะต้องเซฟไฟล์ร่างเหล่านี้ลงในไดเรกทอรี `plans/drafts/` เท่านั้น สำหรับการแก้ไขหน้า Wiki ที่มีอยู่เดิม จะต้องทำเป็น **Full File Copy** (สำเนาเต็มทั้งไฟล์) เสมอ เพื่อให้สามารถตรวจสอบภาพรวมได้ทั้งหมด และยังไม่เขียนลงในไดเรกทอรี `wiki/` แบบถาวรจนกว่าจะผ่านการตรวจสอบ (Verification) และ Main Agent ได้จัดการแก้ไขและติ๊ก (`- [x]`) รายการใน Verification Report จนครบก่อน จึงจะได้รับอนุมัติให้เข้าสู่ Commit Phase
  - **Commit Phase (เฟสยืนยัน)**: ขั้นตอนสุดท้ายของ Orchestration Plan ที่ Agent จะเขียนแบบร่างที่ตรวจสอบแล้วลงในตำแหน่งถาวรใน repository (เช่น `wiki/`) อย่างเป็นทางการ และอัปเดตไฟล์ติดตาม
  - **Post-Completion Cleanup (การทำความสะอาดหลังเสร็จสิ้น)**: เมื่อไฟล์ Orchestration Plan ถูกดำเนินการเสร็จสมบูรณ์ 100% (งานทั้งหมดถูกทำเครื่องหมาย `- [x]` และผ่าน Commit Phase เรียบร้อยแล้ว) Agent จะต้อง **ลบไฟล์ Plan นั้นทิ้ง** ออกจากโฟลเดอร์ `plans/` ทันที เพื่อป้องกันการสะสมของไฟล์ที่ไม่ได้ใช้งาน เนื่องจากประวัติและผลลัพธ์การทำงานได้ถูกบันทึกไว้ใน `logs/log.md` แล้ว
  - **Aborted Plan Handling (การจัดการแผนที่ถูกยกเลิก)**: หากแผนงานถูกยกเลิกกลางคัน (Aborted/Cancelled) Agent จะต้องสรุปสาเหตุและบทเรียนสั้นๆ ลงใน `logs/log.md` จากนั้นให้ **ลบไฟล์ Plan นั้นทิ้ง** ทันทีเช่นกัน ห้ามเก็บสะสมไฟล์แผนงานที่ล้มเลิกไว้ในระบบเพื่อรักษาความสะอาดของโฟลเดอร์ `plans/`
  - **Draft Cleanup (การทำความสะอาดไฟล์แบบร่าง)**: ในกระบวนการลบไฟล์ Plan (ไม่ว่าจะเสร็จสมบูรณ์หรือถูกยกเลิก) Agent จะต้องตามไป **ลบไฟล์แบบร่าง (Drafts)** ทั้งหมดที่เกี่ยวข้องกับแผนงานนั้นๆ ออกจากโฟลเดอร์ `plans/drafts/` ทันทีด้วยเช่นกัน เพื่อรักษาโครงสร้างของพื้นที่ทำงานให้สะอาดแบบเบ็ดเสร็จ
- **Content Visualization (การทำภาพจากเนื้อหา)**: เมื่อพบแนวคิดที่ซับซ้อนหรือยากระหว่างการสร้างหน้าหรือการ Ingestion Agent ควรเริ่มใช้องค์ประกอบแบบโต้ตอบ ภาพแบบต่างๆ (เช่น โฟลว์ชาร์ต Mermaid, ตารางเปรียบเทียบ) หรือตาราง เพื่อทำให้เนื้อหานั้นง่ายต่อความเข้าใจของผู้อ่านมากขึ้น **สำคัญ:** ภาพเหล่านี้จะต้องเป็นส่วนเสริม และห้ามใช้แทนที่คำอธิบายแบบข้อความที่มีความครอบคลุมและมีรายละเอียด ห้ามลดความลึกหรือความยาวของข้อความสรุปเมื่อมีการเพิ่มภาพ
- **Stub Page (หน้าโครง/หน้าว่างชั่วคราว)**: หน้า Wiki ชั่วคราวที่มีแค่ Page Template มาตรฐานและ frontmatter ถูกสร้างขึ้นเพื่อแก้ไข Dead Links (ลิงก์เสีย) จนกว่าจะมีการ Ingestion หรือการสร้างเนื้อหาที่เหมาะสม
- **Entity Alias (นามแฝงของเอนทิตี)**: ในการสร้างหน้าสำหรับลิงก์ที่มีรูปแบบ `[[Target | Alias]]` (เช่น `[[Nate Herk | AI Automation]]`) ให้ถือว่า `Target` คือชื่อเอนทิตีและชื่อไฟล์ที่แท้จริง (เช่น `nate_herk.md`) ส่วน `Alias` เป็นเพียงข้อความแสดงผล หรือบริบท/หัวข้อที่เกี่ยวข้อง ห้ามนำอักขระพิเศษอย่าง `|` มาตั้งเป็นชื่อไฟล์
- **Source (แหล่งข้อมูล)**: หน้า Wiki ใน `wiki/sources` ที่ยึดกฎ 1:1 กับแหล่งข้อมูลอ้างอิงภายนอก (1 Source = 1 File) เสมอ ทำหน้าที่เก็บ Metadata และสรุปเนื้อหาเฉพาะจากแหล่งข้อมูลนั้นเพียงแหล่งเดียว ห้ามนำหลาย URL หรือหลายบทความมารวมไว้ในหน้า Source เดียวกัน
- **Concept (แนวคิด/หัวข้อ)**: หน้า Wiki ใน `wiki/concepts` ที่ทำหน้าที่สังเคราะห์และรวบรวมความรู้จากหลายๆ แหล่ง (Topic Synthesis) แม้ว่าความรู้นั้นอาจจะยังไม่ตกผลึกเป็นหลักการ (Principle) เชิงวิชาการที่ชัดเจนก็ตาม เนื้อหาที่เกิดจากการรวบรวมหลาย Source เข้าด้วยกันเพื่อสรุปเป็นหัวข้อ จะถูกจัดเก็บเป็น Concept
- **Entity (เอนทิตี)**: หน้า Wiki ใน `wiki/entities` ที่ใช้สำหรับสิ่งที่มีตัวตนเฉพาะเจาะจงหรือมีรูปธรรม (Proper Nouns) เช่น ชื่อบุคคล, ชื่อโปรดักต์/เครื่องมือ, หรือชื่อองค์กร ต่อให้มีการสังเคราะห์ข้อมูลจากหลาย Source (Topic Synthesis) แต่ถ้าสิ่งนั้นเป็น Proper Noun ก็ต้องจัดเก็บในหมวด Entity เสมอ

# Agent Behavior Rules (กฎพฤติกรรมของ Agent)

- **Token Optimization & Output Muting (การปรับโทเค็นให้เหมาะสมและปิดการแสดงผลลัพธ์)**: ห้ามแสดงผลเนื้อหาทั้งหมดลงในแชทเพื่อประหยัดโทเค็น ให้เขียนลงในไฟล์ (เช่น โฟลเดอร์ `plans/` หรือไฟล์ `.md` ต่างๆ) แทน และสรุปสั้นๆ ในแชท
- **Raw File Protection (การปกป้องไฟล์ดิบ)**: ห้ามลบไฟล์ต้นฉบับในโฟลเดอร์ `raw/` ภายใต้สถานการณ์ใดๆ ทั้งสิ้น
- **Thai Language Summary (การสรุปเป็นภาษาไทย)**: การสรุปจะต้องเป็นภาษาไทยเท่านั้น หากมีคำศัพท์ทางเทคนิค โปรดใช้การทับศัพท์ (Transliteration)
- **Naming Convention (มาตรฐานการตั้งชื่อ)**: ชื่อไฟล์ Wiki ทั้งหมดจะต้องเป็น `snake_case.md` เสมอ และ **ห้าม** มีคำนำหน้า (Prefix) เช่น `concept_`, `entity_`, หรือ `source_` เด็ดขาด (เช่น ใช้ `nate_herk.md` ไม่ใช่ `Nate Herk.md` หรือ `entity_nate_herk.md`) เนื่องจากเราแยกโฟลเดอร์ตามประเภทไว้แล้ว หากต้องการลิงก์ข้ามหน้าและให้แสดงผลสวยงาม ให้ใช้นามแฝง (Alias) แทนเสมอ (เช่น `[[nate_herk|Nate Herk]]`)
- **Pre-flight Entity Check (การตรวจสอบเอนทิตีก่อนสร้าง)**: ก่อนที่จะสร้างหน้า Wiki ใหม่หรือสร้าง Wiki-link อ้างอิงถึงเอนทิตีใดๆ Agent จะต้องตรวจสอบจากไฟล์ `index.md` (ซึ่งเป็น Source of Truth) เสมอ เพื่อดูว่ามีหัวข้อนั้นถูกสร้างไว้แล้วหรือไม่ (ให้ตรวจสอบแบบ Case-insensitive และระวังเรื่องช่องว่าง) หากพบว่ามีชื่อที่คล้ายกันอยู่แล้ว ให้ใช้ชื่อเป้าหมายเดิมที่ปรากฏใน `index.md` เสมอ ห้ามสร้างไฟล์ใหม่หรือลิงก์ใหม่ที่ซ้ำซ้อน
